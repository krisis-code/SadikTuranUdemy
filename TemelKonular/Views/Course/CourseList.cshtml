@model List<Course>

  @foreach(var kurs in Model)
  {
  <div class="card mb-3"></div>
  <div class="row">
   <div class="col-md-4">
      <img class="img-fluid rounded-start" src="~/img/@kurs.image" alt="" >
    </div>
      <div class="col-md-8">
        <div class="card-body">
        <h5 class="card-title">@kurs.Title</h5>
        <p class="card-text">@kurs.Description</p>
      </div>
      </div>
      </div>
   
 
    }